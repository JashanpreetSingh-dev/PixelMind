## 1. Today summary and primary CTA

- [x] 1.1 Add a today summary line at the top of the home content (e.g. "Reflect tonight" or "X of Y habits done • Reflect tonight" when habits and today data exist)
- [x] 1.2 Place the primary CTA ("Start tonight's reflection" or "Reflect tonight") immediately after the summary line; remove or replace the current aside so there is only one primary CTA on the page

## 2. Content order and layout

- [x] 2.1 Reorder the home page so the structure is: summary line → primary CTA → week grid (grid no longer appears above the CTA on mobile)

## 3. Week grid labels and today column

- [x] 3.1 Replace D1–D7 in the grid header with day labels (e.g. Mon–Sun or numeric dates) derived from the week range
- [x] 3.2 Compute which column corresponds to the current date and apply a distinct style to that column (e.g. border, background, or "Today" label) so today is visually highlighted
